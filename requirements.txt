# Repository entrypoint — install from repo root:
#   pip install -r requirements.txt
#
# The Streamlit app runs from career_agent_platform/ (see README.md).

-r career_agent_platform/requirements_agentic.txt
