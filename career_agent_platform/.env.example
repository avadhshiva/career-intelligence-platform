# Agentic Career Platform — copy to .env and fill in values.
# Never commit real credentials.

# LinkedIn (future browser automation — not used in Phase 5)
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=

# LLM provider (future resume tailoring / outreach — scaffold only)
OPENAI_API_KEY=

# Playwright browser binary path (optional; future browser layer)
PLAYWRIGHT_BROWSER_PATH=
